# ====================1 常見library==============

library(dplyr)  # 數據處理工具集
library(fpp2)  # Forecast工具集
library(nnet)  # 神經網絡工具集

# 關聯規則及可視化
library(arules) 
library(arulesViz)  

library(nutshell)  # data sets used as examples in the book "R in a Nutshell" 

# 文本挖掘工具集
library(tm)  # A framework for text mining applications
library(wordcloud)  # 繪製詞雲
library(RColorBrewer)  # 可視化修飾
library(SnowballC)  # 詞幹提取
library(topicmodels)  # 主題模型
library(qdap)  # 文本量化分析

library(e1071)  # 概率統計數據分析工具包
library(rpart)  # 分类/回归/生存树工具包
library(maptree)  # 决策树可视化工具包
library(ipred)  # bagging工具包
library(mboost)  # boosting工具包
library(randomForest)  # 随机森林工具包
